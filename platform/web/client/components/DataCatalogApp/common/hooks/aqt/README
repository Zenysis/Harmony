This directory contains hooks that produce AQT-style data structures (like the
ones found in models/core/wip) using data from DataCatalog and GraphQL. Most
hooks contain a fragment that can be registered wherever you need to use it.
