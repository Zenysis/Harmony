// @flow

/**
 * Helper function to cast a value to any type. Only use this when you are
 * 100% sure that what you're doing is correct.
 */
export default function cast<T>(value: any): T {
  return value;
}
