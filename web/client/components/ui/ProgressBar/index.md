Simple progress bar built off of bootstrap's builtin loading bar.

With default values:
```jsx
<ProgressBar />
```

Setting the completed percentage:
```jsx
<ProgressBar percentCompleted={80} />
```

With `enabled` set to false
```jsx
<ProgressBar percentCompleted={80} enabled={false} />
```
